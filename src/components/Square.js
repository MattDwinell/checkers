const Square = ({solid}) => {
    let bg = solid ? 'black' : 'white';
    return (
        <div className = 'square' style={{backgroundColor: bg }} >
            
        </div>
    )
}

export default Square
